/**
 * @deprecated ITEM-117 — sin consumidor runtime; SUPERSEDED. La configuración
 * viva del gate de plantillas vive en `template-admin/` (`gate-pre.ts` /
 * `gate-pre-semantic.ts`). SPEC-ONLY. No borrar. Ver
 * `src/lib/rules-engine/INDEX.md`.
 */
// Re-export GO_LIVE_CONFIG from plantillas-engine for convenience
export { GO_LIVE_CONFIG } from './plantillas-engine';
